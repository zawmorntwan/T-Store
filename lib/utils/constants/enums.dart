enum TextSized { small, medium, large }

enum OrderStatus { pending, processing, shipped, delivered, canceled }

enum PaymentMethods { paypal, googlePay, applePay, visa, mastercard, creditCard, paystack, razorPay, paytm, kbzPay, wavePay } 